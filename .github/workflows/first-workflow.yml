name: Understanding Checkout

on:
  push:
    branches:
      - main

jobs:
  learn-checkout:
    runs-on: ubuntu-latest

    steps:
      - name: Before Checkout
        run: ls

      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: After Checkout
        run: ls
